<!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>T</title>
      <link rel="stylesheet" href="css/styles.css">
    </head>
    <body>
        <%- include('partials/header')%>
      <main>
          <a href="/">main</a>
          <form method="POST" action="/query1">
            <h3>Введите период</h3>
            <input name="date-1" type="date"> - 
            <input name="date-2" type="date">
            <button type="submit">отправить</button>
          </form>
          <div class="period">
              <span>Указаный период: <%= reqData["date-1"] %> - <%= reqData["date-2"] %></span>
          </div>
          <div class="organizations">
             <div class="organizations__quantity">
                Число организаций, которым выделены номера за указанный период: <%= orgNumber %>
             </div>
             <div class="oraganizations__cards">
                <% if(queryData.length){%>
                    <table border="1">
                        <tr>
                            <th>Назва органцзації</th>
                            <th>Номер ТС</th>
                            <th>Дата випуску</th>
                        </tr>
                        <% for(let i=0; i<queryData.length; i++){%>
                            <tr>
                                <td><%=queryData[i]["Назва організації"] %></td>
                                <td><%=queryData[i]["Номер ТС"] %></td>
                                <td><%=queryData[i]["Дата випуску"].getFullYear()%>.<%=queryData[i]["Дата випуску"].getMonth()+1%>.<%=queryData[i]["Дата випуску"].getDate()%></td>
                            </tr>
                        <%}%>
                    </table>
                <%}%>
             </div>
          </div>
      </main>
    </body>
    </html>